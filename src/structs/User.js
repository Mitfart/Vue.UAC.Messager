class User {
    constructor(nickname, avatarImg, page, followers) {
        this.nickname = nickname;
        this.avatarImg = avatarImg;
        this.page = page;
        this.followers = followers;
    }
}
export default User